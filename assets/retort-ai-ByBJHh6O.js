const e=`A lightweight Chrome/Edge extension that helps generate context-aware replies using OpenRouter (or other LLM providers). It extracts selected text or visible page content and sends it to an AI model to produce suggested replies you can copy to the clipboard.

## Features
- Generate a reply from selected text or full page content
- Message mode (rewrite a message) and Prompt mode (free-form prompt)
- Tone selection (Polite, Friendly, Professional, etc.)
- Copy suggested reply to clipboard`;export{e as default};
